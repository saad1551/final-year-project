name: fyp-venv
channels:
  - defaults
dependencies:
  - pip
  - jupyter
  - transformers
  - pillow
  - lxml
  - pip:
      - playwright
      - lxml_html_clean
prefix: /Users/saadashraf/miniconda3/envs/fyp-venv
